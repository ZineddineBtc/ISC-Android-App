<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.isc">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Core.EditPostActivity" />
        <activity android:name=".Core.ShowUserProfileActivity" />
        <activity android:name=".Core.TagColleagueActivity" />
        <activity android:name=".Core.IncludeEventActivity" />
        <activity android:name=".Core.PostLevelActivity" />
        <activity android:name=".Core.CreatePostActivity" />
        <activity
            android:name=".Core.CoreActivity"
            android:label="@string/title_activity_core">

        </activity>
        <activity android:name=".LoginSignUp.FinishSignUpActivity" />
        <activity android:name=".LoginSignUp.SignUpActivity" />
        <activity android:name=".LoginSignUp.LoginActivity" />
        <activity android:name=".Entry.EntryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>